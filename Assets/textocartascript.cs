using UnityEngine;

public class textocartascript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public string textocarta;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
